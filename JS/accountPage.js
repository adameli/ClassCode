// renderNavigationLoggedIn();

function renderAccountPage( Username) {

    // temp
    document.querySelector( "main").innerHTML = "<h1>AccountPage</h1>"; 

    /* Recieve username, check if corresponing to logged in user
        if( check if logged in user) {
            true: render logged in userspage "edit and create"
        } 
        else {
            false: render another users page "view + comment"
        }   
    */






}